# Copy to .env for local development. Real environment variables always win.
# These map to the go-flags options in app/config/opts.go (group env-namespaces).
SERVICE_NAME=servicekit-example
APP_ENV=development
LOG_LEVEL=info

# REST and gRPC are both ON by default; set *_DISABLED=true to turn one off.
# HTTP_DISABLED=true
HTTP_ADDR=:8080
HTTP_REQUEST_TIMEOUT=30s
HTTP_SHUTDOWN_TIMEOUT=10s

# GRPC_DISABLED=true
GRPC_ADDR=:9090
GRPC_REFLECTION=true

DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost:5432
DB_NAME=servicekit
DB_SCHEMA=public
DB_DISABLE_TLS=true

# Tracing is off by default; enable with a collector on OTEL_ENDPOINT.
OTEL_ENABLED=false
OTEL_ENDPOINT=localhost:4317
OTEL_INSECURE=true
OTEL_PROBABILITY=1.0
