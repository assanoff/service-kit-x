# Build artifacts
/bin/

# Local environment (committed template: .env.example)
.env

# Go test/coverage output
*.out
coverage.*

# Raw coverage counter data (go build -cover / GOCOVERDIR)
/coverdata/
